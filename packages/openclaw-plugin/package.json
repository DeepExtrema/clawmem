{
  "name": "@clawmem/openclaw",
  "version": "0.1.0",
  "description": "ClawMem memory-slot plugin for OpenClaw — local-first, auditable, reversible.",
  "type": "module",
  "license": "Apache-2.0",
  "author": "tekron",
  "keywords": ["openclaw", "plugin", "memory", "clawmem", "local-first", "long-term-memory"],
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "openclaw.plugin.json"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@clawmem/core": "workspace:*",
    "@sinclair/typebox": "^0.34.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "vitest": "^2.1.0"
  },
  "openclaw": {
    "extensions": ["./dist/index.js"]
  }
}
